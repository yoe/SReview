---
language: perl
perl:
  - "5.20"
  - "5.24"
  - "5.26"
sudo: true
install:
  - sudo add-apt-repository ppa:mc3man/trusty-media -y && sudo apt-get update && sudo apt-get install ffmpeg
  - cpanm --quiet --installdeps --notest .
